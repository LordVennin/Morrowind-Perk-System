# SkillPerkSystem Task/Planning Template

Use this template for new SkillPerkSystem implementation tasks so repository policy is inherited automatically.

## Scope

- Feature area:
- Files/modules expected to change (must be under `SkillPerkSystem/` and optionally repo-root docs):
- Out-of-scope areas:
  - `example_Mod/` (reference-only)
  - `Advanced world map mod example/` (reference-only)

## Starter template + validator deliverables

All starter-template and validator artifacts for this work item must be created/updated only in these locations:

- `SkillPerkSystem/docs/plugins/` (starter template docs + validator command docs/output examples)
- `SkillPerkSystem/scripts/SkillPerkSystem/plugin_examples/` (starter reference files only; non-runtime)
- Optional repo-root documentation updates (policy/usage notes only)

Do not place deliverables in `example_Mod/` or `Advanced world map mod example/`.

## Implementation Plan

1.
2.
3.

## Validation

- [ ] Confirm all implementation changes are inside `SkillPerkSystem/` (or optional repo-root docs only).
- [ ] Confirm starter template docs and validator command/output examples are under `SkillPerkSystem/docs/plugins/` only.
- [ ] Confirm starter reference files are under `SkillPerkSystem/scripts/SkillPerkSystem/plugin_examples/` only.
- [ ] Confirm no files were added/edited/removed in `example_Mod/`.
- [ ] Confirm no files were added/edited/removed in `Advanced world map mod example/`.
- [ ] Run relevant tests/checks.

## Acceptance Criteria

- [ ] Starter template and validator deliverables use SkillPerkSystem-local starter files (not example-based starter packs).
- [ ] Do not modify example folders: `example_Mod/` and `Advanced world map mod example/`.
- [ ] Validator command documentation and expected output examples live only under `SkillPerkSystem/docs/plugins/`.

## PR/Handoff Checklist

- [ ] I confirm this work follows the reference-only folder policy.
- [ ] I confirm there are **no** file changes under `example_Mod/`.
- [ ] I confirm there are **no** file changes under `Advanced world map mod example/`.
- [ ] I confirm active feature implementation changes are under `SkillPerkSystem/` (or optional repo-root docs only).
