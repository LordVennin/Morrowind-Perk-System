-- Runtime bootstrap script for the bundled sample perk pack.
-- Registration metadata is loaded separately by SkillPerkSystem's plugin loader
-- from scripts.VenninsPerks.skillperk_manifest.
return {
    eventHandlers = {},
    engineHandlers = {},
}
